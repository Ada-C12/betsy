<h2>
<% if @user.merchant_name %>
  <%=@user.merchant_name%>
<% else %>
  <%= @user.username%>
<% end %>
's Shop!
</h2>

<% if @user.products.count > 0 %>
  <% @user.products.each do |product| %>
    <ul>
      <%= image_tag product.img_url, alt: product.name + "image"%>
      <li>name: <%=link_to product.name, product_path(product.id) %></li>
      <li>product: <%=product.price%></li>
      <li>stock: <%=product.quantity%></li>
      <li>description: <%=product.description%></li>
    </ul>
  <% end %>
<% else %>
  <p>Merchant's fruit stand is empty.</p>
<% end %>
