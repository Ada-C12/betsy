<!DOCTYPE html>
<html>
  <head>
    <title>fruitsy</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <ul>
    <%if session[:user_id]%>
      <% user = User.find_by(id: session[:user_id])%>
      <li><%= link_to user.username, current_user_path, class: "btn btn-primary" %>
      <li><%= link_to "Log out", logout_path, method: "delete", class: "btn btn-primary"%>
    <%else %>
      <li><%= link_to "Login with Github", github_login_path, class: "btn btn-primary "%></li>
    <%end %>
    </ul>



    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="#">fruitsy</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <%= link_to "Home", root_path, class: "nav-link"%>
          </li>
          <li class="nav-item">
            <%= link_to "Shop", products_path, class: "nav-link"%>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Categories
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <a class="dropdown-item" href="#">Apple</a>
              <a class="dropdown-item" href="#">Blueberry</a>
              <a class="dropdown-item" href="#">Peach</a>
              <a class="dropdown-item" href="#">Strawberry</a>

              <div class="dropdown-divider"></div>
              <a class="dropdown-item" href="#">Something else here</a>
            </div>
          </li>
          <%if session[:user_id]%>
          <li class="nav-item">
            <%= link_to "View my store", current_user_path, class: "nav-link"%>
          </li>
          <%end%>
        </ul>
        <form class="form-inline my-2 my-lg-0">
          <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">search</button>
        </form>
      </div>
    </nav>
    <%if flash%>
      <section class="alert__container">
      <ul>
        <% flash.each do |name, message| %>
          <li><%=name.capitalize%>: <%=message%></li>
        <%end%>
      </ul>
      </section>
    <%end %>
    <%= yield %>
  </body>
</html>
